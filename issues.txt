Known problems
==============

General
-------
* Refactor filter_jeff.py and diplacement_jeff.py
* Removing all plugins from project causes error
* Move commonly used widgets and models to util.qt
* Tweak progress dialogs to behave nicely
* Changing or moving the project file, the project directory
  and its contents might cause error

User-interface
---------------
* The selectionModel used to list files always resets to the topmost file when navigating to a new plugin

Data management
---------------
* Manage 'manipulations' correctly
* The source field tells you what video was in view for bulk processing, not which video it is from

alignment + concat
------------------
* Add error checks especially considering
  conflicting array shapes

Memory-efficient processing
---------------------------
* Change all plugins to process efficiently
